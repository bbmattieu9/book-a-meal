let MenuService;_e64‍.x([["default",()=>_e64‍.o]]);_e64‍.w("../services/menu-service",[["default",["MenuService"],function(v){MenuService=v}]]);

const MenuController = {
  fetchMenuList(req, res) {
    const allMenus = MenuService.fetchMenuList();
    return res
      .json({
        status: 'success',
        data: allMenus,
      })
      .status(200);
  },

  add(req, res) {
    const createThisNewMenu = req.body;
    const theNewlyCreatedMenu = MenuService.addMenu(createThisNewMenu);
    return res
      .json({
        status: 'success',
        data: theNewlyCreatedMenu,
      })
      .status(201);
  },
};
_e64‍.d(MenuController);
