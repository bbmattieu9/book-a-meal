let dummyData,Menu;_e64‍.x([["default",()=>_e64‍.o]]);_e64‍.w("../utils/dummyData",[["default",["dummyData"],function(v){dummyData=v}]]);_e64‍.w("../models/menu.model",[["default",["Menu"],function(v){Menu=v}]]);


const MenuService = {
  fetchMenuList() {
    const theMenu = dummyData.menu.map((menu) => {
      const newMenu = new Menu();
      newMenu.id = menu.id;
      newMenu.mealId = menu.mealId;
      newMenu.date = menu.date;
      return newMenu;
    });
    return theMenu;
  },

  addMenu(menu) {
    const menuId = menu;
    const lengthOfMenu = dummyData.menu.length;
    const lastMenuId = dummyData.menu[lengthOfMenu - 1].id;
    const newId = lastMenuId + 1;
    menuId.id = newId;
    dummyData.menu.push(menu);
    return menu;
  },
};
_e64‍.d(MenuService);
